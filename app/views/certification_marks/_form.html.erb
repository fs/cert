<%= semantic_form_for([parent, resource]) do |form| %>
  <%= form.inputs do %>
    <%= form.input :certification %>
    <%= form.input :skill %>
    <%= form.input :mark, :as => :radio, :collection => CertificationMark::MARKS %>
  <% end %>

  <%= form.buttons %>
<% end %>
