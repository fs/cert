<%= semantic_form_for([@user, @certification, resource], :remote => true) do |form| %>
  <%= form.inputs do %>
    <%= form.input :skill %>
    <%= form.input :mark, :as => :radio, :collection => CertificationMark::MARKS %>
    <%= form.input :comment %>
  <% end %>

  <%= form.buttons %>
<% end %>
